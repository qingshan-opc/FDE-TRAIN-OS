# 第 5 节 · 运维发布计划卡

## 发布输入

- `RUNBOOK.md`；
- `test-report.md`；
- `release-candidate.md`；
- `.env.example`；
- 确定版本的项目文件。

## 七个环境问题

```text
发布到哪里
是否公开访问
需要哪些环境变量
数据怎样备份
使用哪个端口
可接受多久停止服务
失败时怎样回退
```

## 两条安全线

- `.env.example` 只写变量名与示例，不写真实密钥；
- 测试未通过或版本不确定时停止发布。
