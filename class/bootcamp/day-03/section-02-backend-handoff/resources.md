# 第 2 节 · 后端交接卡

## 五项复述

1. 核心业务规则；
2. 页面展示数据；
3. 用户提交数据；
4. 加载、空、成功、错误状态；
5. 未确认问题。

## 五个澄清问题

```text
数据来源 → 等待
核心对象 → 等待
访问权限 → 等待
隐私限制 → 等待
Python 环境 → 等待
```

## 环境检查

在 QRAE 终端运行 `python --version`；若无结果再运行 `py --version`。不要自行下载未知安装包。
