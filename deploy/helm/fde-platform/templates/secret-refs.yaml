# Secret refs (no credentials in chart).
# Create the Secret named by values.existingSecret before install, e.g.:
#
#   kubectl -n fde create secret generic fde-platform-secrets \
#     --from-literal=JWT_SECRET='replace-me' \
#     --from-literal=DATABASE_URL='postgresql://fde:...@fde-pg-rw:5432/fde' \
#     --from-literal=S3_ACCESS_KEY='...' \
#     --from-literal=S3_SECRET_KEY='...' \
#     --from-literal=LINGZHI_API_KEY='' \
#     --from-literal=ANYCODE_API_TOKEN=''
#
# Workloads reference keys via secretKeyRef (see api/worker Deployments).
{{- /* intentionally no Secret resource — credentials stay out of git */ -}}
