# Curriculum 合约

运行时从 YAML seed 进 Postgres；**源文件在** `class/bootcamp/day-NN/`。

| 路径 | 说明 |
|------|------|
| `examples/day-NN-curriculum.yaml` | 由 `scripts/build_v07_week1_contracts.py` 生成（Day 1–10） |

改课流程：改 `class/bootcamp` → 跑 build 脚本 → seed（见 [`scripts/README.md`](../scripts/README.md)）。
