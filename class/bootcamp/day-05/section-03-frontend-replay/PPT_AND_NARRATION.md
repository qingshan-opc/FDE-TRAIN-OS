# 第五天 · 第三节 · 前端按合同完成改动

路径：`class/bootcamp/day-05\section-03-frontend-replay/video/`  
PPT：`video/index.html`（**仅讲解图 + 概念要点，无口播正文**）  
分词稿：`video/scripts/narration/`

> 口播以分词稿为准 · TTS 后需重跑 `patch_section_video_timing.py`

---

## 01 · open

**PPT（屏幕）**
- 前端接收第三棒
- 产品合同
- 交互合同
- 现有代码

**口播**
> 原型已经确认。现在切换到前端岗位。它要读取产品合同和界面合同。现有前端代码也是必须读取的输入。人工智能不能跳过检查直接重写页面。前端先理解第一版怎样运行。接着再决定最小改动放在哪里。

文稿：`video/scripts/narration/01-open.txt`

---

## 02 · inspect

**PPT（屏幕）**
- 先检查旧结构
- 入口文件
- 相关组件
- 请求位置与复用

**口播**
> 第一段只检查现有结构。前端要找到页面入口和相关组件。当前请求从哪里发出也要说明。能够复用的状态和样式要尽量保留。这个阶段不修改任何代码。先看清旧结构能够避免创建重复文件，也能减少无关重构。

文稿：`video/scripts/narration/02-inspect.txt`

---

## 03 · plan

**PPT（屏幕）**
- 最小改动清单
- 修改哪些文件
- 为什么修改
- 哪些保持不变

**口播**
> 第二段只提出改动清单。每个准备修改的文件都要说明原因。保持不变的部分也要列出。需要后端配合的请求合同要单独写清。输出以后智能体停止。你确认范围没有失控以后，再回复确认前端改动。

文稿：`video/scripts/narration/03-plan.txt`

---

## 04 · implement

**PPT（屏幕）**
- 按合同实现
- 只做确认范围
- 四状态
- 拒绝无关重构

**口播**
> 人工闸门通过以后才能执行修改。前端只实现界面交接中的元素和动作。加载与空数据需要真实反馈。成功和错误状态也不能遗漏。它不得替后端实现核心业务规则。目录重组和技术替换如果与需求无关，本次全部禁止。

文稿：`video/scripts/narration/04-implement.txt`

---

## 05 · boundary

**PPT（屏幕）**
- 前端岗位边界
- 页面与交互
- 组装请求
- 规则交给后端

**口播**
> 前端负责用户看到和操作的部分。它也负责按照合同组装请求。不过最终业务规则和数据存储属于后端。发现合同缺少字段时不要自行发明。前端应该把问题写入交接文件。明确边界能让后续联调更快定位责任。

文稿：`video/scripts/narration/05-boundary.txt`

---

## 06 · ai-era

**PPT（屏幕）**
- AI 时代前端
- 定位和生成更快
- 人控制范围
- 真实运行验收

**口播**
> 人工智能可以快速定位文件并生成页面代码。开发速度确实变快了。人的责任会更多集中在改动范围和真实运行。你要确认它没有修改无关文件。还要打开页面查看结果。代码看起来正确不能代替浏览器中的真实证据。

文稿：`video/scripts/narration/06-ai-era.txt`

---

## 07 · selfcheck

**PPT（屏幕）**
- 前端自检
- 页面能打开
- 四状态
- 控制台与请求

**口播**
> 实现完成后运行项目已有检查。接着打开真实页面验证四种状态。浏览器控制台不能出现新增报错。请求面板要显示约定的方法和字段。后端尚未完成时可以记录待联调状态，但不能把模拟结果写成真实通过。

文稿：`video/scripts/narration/07-selfcheck.txt`

---

## 08 · handoff

**PPT（屏幕）**
- 前端交付
- 修改文件
- 请求合同
- 证据与后端待办

**口播**
> 前端最终生成验收文件和交接文件。修改过的文件要逐一列出。页面入口和请求合同必须清楚。四种状态的实际结果与证据路径也要保留。需要后端完成的事项单独列出。下一棒才能准确接住任务。

文稿：`video/scripts/narration/08-handoff.txt`

---

## 09 · close

**PPT（屏幕）**
- 第三棒完成
- 交互落到页面
- 不越过岗位边界
- 下一棒后端

**口播**
> 到这里交互合同已经落到真实页面。前端没有重做整个系统。它也没有越权处理业务规则。你再次完成了结构检查和改动计划。人工确认与执行交接也已经完成。下一节切换到后端岗位。产品规则和前端请求将连到真实服务与数据。

文稿：`video/scripts/narration/09-close.txt`

---

## 评审清单

- [x] PPT 无口播正文，仅图 + 概念卡/表
- [x] 口播与 `narration/*.txt` 一致
- [ ] TTS 后 patch 时间轴并重渲
