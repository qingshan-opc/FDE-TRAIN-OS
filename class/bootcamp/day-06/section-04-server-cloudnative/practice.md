# 第 3 节 · 练习

## 知识确认（3′）

1. 用一句话说清服务器是什么，以及它和你笔记本的两个差别。
2. 「容器」和「集装箱」的类比里，箱子里装的是什么？船是什么？
3. 用买车 / 打车解释自建机房和 Serverless 的区别。
4. 为什么服务器只有命令行、没有图形界面？

## 实操任务（10′）

**预习命令手册**：打开资源区「命令手册 · 服务器日常八句」，把八句各读一遍——等下本节「实验」步骤要全部真敲：
`pwd` / `ls -l` / `mkdir -p app/logs` / `cd app` / `curl localhost:8000/healthz` / `tail -f logs/server.log` / `chmod +x deploy.sh` / `docker ps`

边读边猜：每个命令敲下去，屏幕上会出现什么？

## 完成标志

- 五站演进按顺序背出，且每站能说一句「解决了什么麻烦」
- 八句命令能看着手册说出各自用途，其中 6 句能默写
- 能解释 `curl` 返回 200 意味着什么、返回「拒绝连接」意味着什么
