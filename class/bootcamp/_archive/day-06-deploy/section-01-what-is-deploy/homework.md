# 第 1 节 · 作业（随堂完成）

1. 管线找位置五题 + 加试题，交导师验收；
2. **仓库体检**（写 `reflection.md`）：逐项确认——
   - `git log` 最新 commit 是否包含所有工作（没进 Git 的不存在）；
   - 仓库里有没有任何密钥/令牌（搜「token」「key」「sk-」试试）；
   - `.gitignore` 里有没有 `.env`。

> 体检发现密钥进过 Git：立刻按「吊销换新」处理（Day 5 三角），并告诉导师。
