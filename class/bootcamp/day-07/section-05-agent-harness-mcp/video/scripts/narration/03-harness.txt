Agent 外面那层壳，叫 Harness，可以叫编排框架。它管工具表、记忆、规划、重试、日志、权限。同一个大模型，套 Cursor 的 Harness 能改十个文件；套个简陋脚本可能只会一问一答——差的不是「脑子」，是「骨架」。第一节说的编排层，很大一块就是这类东西。有同学可能会问：那 Harness 是不是就是产品？接近——产品把 Harness 产品化了。你要记住：别只问「用哪个型号」，先问外面这层骨架靠不靠谱。日志和重试看起来土，但没有它们，Agent 出事你查不到。下一页看模型怎么伸手调工具，对吧。
