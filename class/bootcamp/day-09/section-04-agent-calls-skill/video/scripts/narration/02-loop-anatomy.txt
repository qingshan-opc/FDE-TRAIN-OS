循环长什么样？用人话说：消息列表进模型，模型要么直接给最终答案，要么甩出一串「要调哪些工具」的请求。有调用，你就按名单去执行，把工具结果再塞回消息列表，进入下一轮；没有调用，这一轮结束。有同学可能会问：那和我手动点一次 Skill 有啥区别？区别在「回灌」——结果回到模型眼前，它才能决定继续读文件、再调 Skill，还是收工。听见了吗？人话走一圈：想 → 做 → 看 → 再想。屏幕上那些 tool_calls、dispatch、for turn，是代码里的函数名，对着抄即可——别跳过回灌这一步，嘛。
