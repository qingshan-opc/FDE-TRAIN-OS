# 第五天 · 第三节 · 前端按合同完成改动

路径：`class/bootcamp/day-05\section-03-frontend-replay/video/`  
PPT：`video/index.html`（**仅讲解图 + 概念要点，无口播正文**）  
分词稿：`video/scripts/narration/`

> 口播以分词稿为准 · TTS 后需重跑 `patch_section_video_timing.py`

---

## 01 · open

**PPT（屏幕）**
- 前端接收第三棒
- 产品合同
- 交互合同
- 现有代码

**口播**
> 同学们好，我是你们的老师404。原型确认了，切到前端岗位。它要读产品合同、界面合同，现有前端代码也是必读输入。AI 不能跳过检查直接重写页面——先搞懂第一版怎么跑，再决定最小改动落在哪。
文稿：`video/scripts/narration/01-open.txt`

---

## 02 · inspect

**PPT（屏幕）**
- 先检查旧结构
- 入口文件
- 相关组件
- 请求位置与复用

**口播**
> 第一段，只检查现有结构：找到页面入口和相关组件，说清当前请求从哪发出，能复用的状态和样式尽量保留。这个阶段一行代码都不改。先看清旧结构，才不会造出重复文件，也能少很多无关重构。
文稿：`video/scripts/narration/02-inspect.txt`

---

## 03 · plan

**PPT（屏幕）**
- 最小改动清单
- 修改哪些文件
- 为什么修改
- 哪些保持不变

**口播**
> 第二段，只提改动清单：每个准备修改的文件说明原因，保持不变的部分也列出来，需要后端配合的请求合同单独写清。输出后智能体停，你确认范围没失控，再回复确认前端改动。闸门过了，才动手。
文稿：`video/scripts/narration/03-plan.txt`

---

## 04 · implement

**PPT（屏幕）**
- 按合同实现
- 只做确认范围
- 四状态
- 拒绝无关重构

**口播**
> 动手以后，纪律不变：前端只实现界面交接里的元素和动作；加载和空数据要有真实反馈，成功和错误状态不能漏；不许替后端实现核心业务规则；跟需求无关的目录重组、技术替换，本次全禁。写完，边界再强调一遍。
文稿：`video/scripts/narration/04-implement.txt`

---

## 05 · boundary

**PPT（屏幕）**
- 前端岗位边界
- 页面与交互
- 组装请求
- 规则交给后端

**口播**
> 边界就一条线：前端管用户看到和操作的，管按合同组装请求；最终业务规则和数据存储，归后端。发现合同缺字段，别自己发明——把问题写进交接文件。边界清楚，后面联调才能快速定位责任，对吧。
文稿：`video/scripts/narration/05-boundary.txt`

---

## 06 · ai-era

**PPT（屏幕）**
- AI 时代前端
- 定位和生成更快
- 人控制范围
- 真实运行验收

**口播**
> AI 定位文件、生成页面代码都很快，开发速度确实上去了。但人的责任更集中到两处：改动范围——确认它没动无关文件；真实运行——打开页面亲眼看。代码看着对，代替不了浏览器里的真实证据。看完，做自检。
文稿：`video/scripts/narration/06-ai-era.txt`

---

## 07 · selfcheck

**PPT（屏幕）**
- 前端自检
- 页面能打开
- 四状态
- 控制台与请求

**口播**
> 自检怎么做？先跑项目已有的检查，再打开真实页面验证四种状态。浏览器控制台不能有新增报错，请求面板要显示约定的方法和字段。后端还没好，就记待联调——但不能把模拟结果写成真实通过。
文稿：`video/scripts/narration/07-selfcheck.txt`

---

## 08 · handoff

**PPT（屏幕）**
- 前端交付
- 修改文件
- 请求合同
- 证据与后端待办

**口播**
> 前端最后交两份文件：验收文件和交接文件。改过的文件逐一列出，页面入口和请求合同写清楚，四种状态的实际结果和证据路径保留，需要后端完成的事单独列。这样下一棒，才能稳稳接住。
文稿：`video/scripts/narration/08-handoff.txt`

---

## 09 · close

**PPT（屏幕）**
- 第三棒完成
- 交互落到页面
- 不越过岗位边界
- 下一棒后端

**口播**
> 同学们，本节先到这里。到这儿，交互合同落到了真实页面：前端没重做整个系统，也没越权碰业务规则；结构检查、改动计划、人工确认、执行交接，你又一次走全了。下一节切后端，把产品规则和前端请求接到真实服务与数据上。
文稿：`video/scripts/narration/09-close.txt`

---

## 评审清单

- [x] PPT 无口播正文，仅图 + 概念卡/表
- [x] 口播与 `narration/*.txt` 一致
- [ ] TTS 后 patch 时间轴并重渲
