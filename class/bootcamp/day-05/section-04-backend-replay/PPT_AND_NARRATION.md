# 第五天 · 第四节 · 后端完成接口与数据联调

路径：`class/bootcamp/day-05\section-04-backend-replay/video/`  
PPT：`video/index.html`（**仅讲解图 + 概念要点，无口播正文**）  
分词稿：`video/scripts/narration/`

> 口播以分词稿为准 · TTS 后需重跑 `patch_section_video_timing.py`

---

## 01 · open

**PPT（屏幕）**
- 后端接收第四棒
- 产品规则
- 前端合同
- 原运行合同

**口播**
> 前端已经完成交接。现在切换到后端岗位。它要读取产品规则和前端请求合同。原来的接口说明与数据结构也不能遗漏。运行手册告诉它服务怎样启动。后端先把这些输入对齐，再判断本次应该修改哪些服务能力。

文稿：`video/scripts/narration/01-open.txt`

---

## 02 · inspect

**PPT（屏幕）**
- 先检查现有后端
- 相关入口
- 当前规则
- 数据结构与兼容

**口播**
> 第一段仍然只做检查。后端要找到相关接口入口和业务规则。现有数据表能否支持新需求也要判断。原接口正在被哪些页面使用需要说明。这个阶段不修改代码。先看清依赖关系可以避免修好新功能却破坏第一版。

文稿：`video/scripts/narration/02-inspect.txt`

---

## 03 · contract

**PPT（屏幕）**
- 接口方案
- 方法与路径
- 字段与响应
- 错误合同

**口播**
> 接着生成本次接口说明。请求方法和路径要明确。输入字段与返回字段也要写清。成功结果和错误结果必须同时约定。前端提出的合同如果与产品规则冲突，后端要停止并要求确认。它不能用代码实现偷偷改变需求。

文稿：`video/scripts/narration/03-contract.txt`

---

## 04 · data

**PPT（屏幕）**
- 数据变更判断
- 优先复用
- 兼容旧数据
- 准备回退

**口播**
> 数据方案需要单独记录。现有字段能够满足时就不修改数据表。这个结论也要写入文件。确实需要新增字段时，要说明旧数据怎样兼容。迁移失败时如何恢复也要提前写清。数据风险没有确认以前不能执行修改。

文稿：`video/scripts/narration/04-data.txt`

---

## 05 · gate

**PPT（屏幕）**
- 确认后端改动
- 接口
- 数据
- 兼容与回退

**口播**
> 接口和数据方案输出后智能体先停止。你要检查请求是否与前端一致。还要检查数据变更是否必要。原接口兼容和回退方法也要逐条阅读。全部通过以后回复确认后端改动。这道闸门通过后才能开始修改代码。

文稿：`video/scripts/narration/05-gate.txt`

---

## 06 · implement

**PPT（屏幕）**
- 实现业务能力
- 保持原接口
- 只改相关模块
- 密钥不进代码

**口播**
> 后端只实现本次确认的业务规则。原接口和旧数据继续保持可用。无关模块不得顺便重构。真实密钥不能进入代码或证据文件。人工智能可以生成样板代码，不过规则正确性和数据安全仍然需要人负责。

文稿：`video/scripts/narration/06-implement.txt`

---

## 07 · integration

**PPT（屏幕）**
- 真实联调
- 启动与健康
- 成功和错误
- 前端真实调用

**口播**
> 实现完成后先启动服务并检查健康接口。接着执行成功请求和错误请求。需要保存的数据要在重启后复查。最后打开前端真实页面完成调用。只用请求工具看到成功还不算完整联调。合同两端必须同时工作。

文稿：`video/scripts/narration/07-integration.txt`

---

## 08 · handoff

**PPT（屏幕）**
- 后端交付
- 运行结果
- 证据路径
- 测试入口与限制

**口播**
> 后端最终生成验收文件和交接文件。启动命令与实际结果要写清。成功请求需要保留证据。错误处理和持久化证据也要能够打开。已知限制不能隐藏。测试岗位要从交接中找到运行入口。检查范围和当前风险也必须明确。

文稿：`video/scripts/narration/08-handoff.txt`

---

## 09 · close

**PPT（屏幕）**
- 第四棒完成
- 页面连到服务
- 服务连到数据
- 下一棒测试

**口播**
> 到这里页面已经连接真实服务。服务也按照合同处理规则与数据。你再次完成了前后端边界和真实联调。下一节我们切换到测试岗位。它不会只验证新功能，还会检查本次改动有没有破坏第一版能力。

文稿：`video/scripts/narration/09-close.txt`

---

## 评审清单

- [x] PPT 无口播正文，仅图 + 概念卡/表
- [x] 口播与 `narration/*.txt` 一致
- [ ] TTS 后 patch 时间轴并重渲
